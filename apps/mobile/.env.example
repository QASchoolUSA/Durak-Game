# Convex backend URL for online play (required for "With friends" mode)
# Get this from your Convex dashboard after running: pnpm --filter @durak/mobile convex:deploy
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Deploy key — set in .env.local only, never commit
# CONVEX_DEPLOY_KEY=
